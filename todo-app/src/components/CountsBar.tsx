/*
  components/CountsBar.tsx

  役割：合計／未完／完了のカウント表示と、Clear Completed ボタンを提供する。

  Props の例：
  type Props = {
    counts: { total: number; active: number; completed: number }
    onClearCompleted: () => void
  }
*/

export default function CountsBar(/* props: Props */) {
  // const { counts, onClearCompleted } = props
  // return (
  //   <div>
  //     <span>Total: {counts.total}</span>
  //     <span>Active: {counts.active}</span>
  //     <span>Completed: {counts.completed}</span>
  //     <button onClick={onClearCompleted}>Clear Completed</button>
  //   </div>
  // )
  return <div>{/* CountsBar UI */}</div>
}
